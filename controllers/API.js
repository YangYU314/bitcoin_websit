/*
const gdax = require('gdax');
const publicClient = new gdax.PublicClient();

publicClient.getProductHistoricRates('BTC-USD', function (err, res, data) {
    console.log(data[0]);
    //console.log(data.length);
})

publicClient.getTime().then(data => console.log(data));*/
